[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "basiliskx"
version = "0.1.0"
description = "BASILISK-X spacecraft simulation and visualization utilities"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]